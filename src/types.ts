export type Worker = {
  id: string;
  name: string;
  birth: string;
  gender: string;
  role: string;
  company?: string;
  division?: string;
  head?: string;
};

export type Head = {
  id: string;
  name: string;
};
